# AGENTS.md — قواعد مرجعية ثابتة (يجب تطبيقها في كل تحديث قادم)

> هذه القواعد مُلزِمة لكل تعديل على هذا المشروع. لا تُخالَف إلا بطلب
> صريح من المستخدم. عند أي تعديل مستقبلي، يجب احترام كل بند هنا.

---

## 0) الخصوصية التشغيلية — لا يكشف البروكر أننا بوت ولا منطق "الفجوة"

البروكر (FP Markets / cTrader) يرى فقط: الرمز، الاتجاه، الحجم، SL/TP،
الملصق (label)، التعليق (comment)، وتوقيت الأوامر. لذلك:

1. **ممنوع** وضع أي كلمة تدل على البوت أو على المنطق في أي حقل يُرسل
   للبروكر (`label`, `comment`, `clientId`…). ممنوع ذكر:
   `gap`, `yahoo`, `GC=F`, `momentum`, `catch_up`, `z-score`, `bot`,
   `auto`, `reversion`, `lead/lag`, أو أي مصدر خارجي.
2. الملصقات يجب أن تكون **أسماء بشرية مقروءة** (مثال: `gold main`،
   `afx one`) — لا رموز عشوائية آليّة (`A1B2C3`). الدالة المرجعية:
   `main._human_label()` و `cbot.random_label()` تستدعيها.
3. لا نُرسل أي تعليق تقني (`comment=""` دائماً).
4. لا يُكتب في السجلات المرفوعة للمستودع (CSV/JSON) ما يكفي لربط
   القرار بالبوت إن كان الريبو عامًّا — الأسماء محايدة.

## 1) لا بصمة آلية في الأوامر

- **SL/TP**: لا مسافات متطابقة حرفياً كل صفقة. يُضاف تشتّت بسيط
  `HUMAN_SL_TP_JITTER_USD` عند الفتح، وتشتّت ثابت لكل صفقة على عتبات
  الربح/التريلنج عبر `_stable_jitter(positionId)`. **استثناء يجب أن
  يبقى صارماً بلا تشتت: `MAX_LOSS` وحدود الأمان و`price_anomaly`.**
- **الحجم (volume)**: يُشوَّش قليلاً `HUMAN_VOLUME_JITTER_FRAC` حول
  الأساس (يبقى في نطاق آمن). اللوت الأساسي يبقى 0.01 كما ضبطه المستخدم.
- **التهدئة (cooldown)**: ليست ثابتة بالثانية — تشتّت ±15%.
- **فترة الجرد (poll)**: `GLOBAL_POLL_SEC` يُشوَّش بـ `HUMAN_POLL_JITTER_SEC`
  (عبر `main._poll_jitter()`) — لا نبض آلي منتظم.

## 2) سلوك بشري

- **تأخير رد فعل قبل التنفيذ**: `HUMAN_REACTION_SEC_MIN/MAX` قبل الفتح،
  ونصف ذلك قبل الإغلاق الاختياري (ربح/تريلنج/ارتداد). **لا تأخير أبداً
  على حدود الأمان (max_loss / price_anomaly).**
- **لا نلتقط كل إشارة**: احتمال `HUMAN_SKIP_SIGNAL_PROB` لتخطي إشارة
  صالحة (`_skip_signal()`).
- كل toggle تحت `HUMANIZE_ON` (env: `STRAT_HUMANIZE_ON`).

## 3) قواعد التداول والمخاطر (ثابتة)

- **القاعدة الجذرية 2026-09-16 — لا صفقة بلا وقوف سيرفر**: كل طلب فتح
  يجب أن يحمل `SL` و`TP` مقبولين لدى الوسيط من لحظة التنفيذ (FP يُرفض
  تعديلها بعد ذلك). أي صفقة وصلت case `sltp_set=False` تُغلق قسراً ذاتياً
  أول فرصة بعد 120 ثانية (سبب `no_broker_stops`) — حتى لو توقف البوت،
  السيرفر هو الضامن: الخسارة توقَف عند `SL` والربح يُثبَّت عند `TP`، فلا
  تبقى صفقة معلقة بلا حماية أبداً.
- **`MAX_HOLD_HOURS` = 1.0 افتراضياً** (سقف إضافي للصفقة الجامدة بين
  SL/TP؛ env `STRAT_MAX_HOLD_HOURS` يتجاوزه).
- اللوت **0.01 فقط** وممنوع تغييره من env أو أي مصدر (نقطة = $1).
- العتبات (عند لوت 0.01): `PROFIT_TARGET_USD=1.60`،
  `TRAILING_ARM_USD=1.40` (2026-09-16: 0.75→1.40)،
  `TRAILING_BACK_USD=0.45` (2026-09-16: 0.30→0.45)،
  `MAX_LOSS_USD=3.0`، `SL_AFTER_ENTRY_USD=2.0` (2026-09-16: 2.5→2.0)،
  `MOMENTUM_MIN_USD=1.50` (2026-09-16: 1.20→1.50).
- **دخول قوي 2026-09-21**: لا فتح إلا مع تأكيد **ثلاث دورات** — إشارة
  اللحاق قوية (≥ العتبة) عند الدورة الحالية **و** الدورتين السابقتين
  بنفس الاتجاه (`_prev_catch_up`/`_prev2_catch_up`)، قتل دخول الومضات
  (whipsaw). العتبة **تُبقي 1.50 ولا تُرفع لـ1.80** — بقرار المستخدم
  "تقوية دون تقليل عدد الصفقات"؛ التأكيد الثلاثي يؤخر الدخول ~15 ثانية
  فقط ولا يحرم من الدخولات الشرعية في الاتجاه القوي المستمر.
- **نظام الإغلاق 2026-09-16**: لا إغلاق مبكر للربح. ستوب سيرفر 2.0$ +
  TP سيرفر `max(1.40, 0.50×|catch_up|)` هما الطبقتان التنفيذيتان الأصليتان؛
  التريلنج (arm 1.40/back 0.45) يحمي الفائض فوق الهدف فقط؛
  `profit_target` (1.60) والحد الساعي (1h) شباك أمان ديناميكية؛
  **z_revert أُزيل** — مركز القناة نقطة بدء اللحاق لا خروجه (44 صفقة
  متوسطة +0.48 = أرباح مبتورة).
- `MAX_DAILY_LOSS_USD` و`MAX_CONSECUTIVE_LOSSES` معطّلان عملياً (99999)
  بقرار المستخدم — التداول مستمر.
- مصادر السعر: **Yahoo GC=F هو المصدر الأساسي** (الأسرع والأكثر سيولة،
  يقود)، وgold-api سبوت fallback. الفرق البنيوي بين GC=F والسبوت ~37$
  (آجلة مقابل سبوت) — **لا يُستخدم كإشارة وليس شذوذاً**.
- إشارة الدخول: `catch_up = yahoo_momentum − platform_momentum` (لحاق
  المنصة بياهو). TP = 45% من `|catch_up|` بحد أدنى 1.00$.
- حارس الشذوذ على **قفزة سعر المنصة** فقط (`platform_momentum`) بعتبة
  `PRICE_JUMP_ANOMALY_USD=6.0` — قفزة ياهو ليست شذوذاً (سوق حقيقي).

## 3ب) اكتشاف العمولة والسبريد (2026-09-15 — يُطبق دائماً)

- **السبريد يُقيس حياً من cTrader bid/ask** كل دورة
  (`result["spread_usd"]`، يُخزَّن في state كـ `_last_spread_usd`) — لا
  تقدير ثابت. يُخصم من صافي الربح في كل حساب.
- **العمولة**: أولاً من كائن الصفقة (`pos.commission`) إن وُجدت؛ وإلا
  معايرة ذاتية من متوسط الصفقات المغلقة
  (`state.perf.measured_commission_usd`، تُحدَّث في `_record_close`)؛
  وإلا تقدير `TRADING_FEES_PER_TRADE_LOT` لكل لوت.
- **قرار الفتح يخضع للتكلفة**: `abs(catch_up)` يجب أن يتجاوز
  (سبريد+عمولة)×2 (حتى لا تكون الصفقة خاسرة من البداية)، ويمنع الدخول
  إذا كان السبريد الحي ≥ `SPREAD_GUARD_USD` (سبب `spread_wide`).
- الدالة المرجعية: `position_fees_usd(pos, md, result=None,
  state=None)` و `dynamic_pnl_usd(..., result=None, state=None)`.
- كل طبقات الإغلاق تستخدم **الصافي بعد التكلفة** (لا الربح الإجمالي).

## 4) هندسة/جودة

- كل تعديل: شغّل `python -m py_compile config.py main.py live.py cbot.py gold_price.py`.
- لا تكسر توقيعات الدوال العامة (`_human_label`, `platform_momentum`,
  `yahoo_momentum`, `_poll_jitter`, `_jittered_volume`, `_skip_signal`).
- لا تلتزم/تدفع (commit/push) إلا بطلب صريح من المستخدم.
- عند الدفع: `git pull --rebase origin main` أولاً (توجد commits بيانات).
